import os








def admin_start():
    """ This launches a UI for admins to perform tasks on the pipeline, such as pushing pipeline tools from dev build to production build """

    print("*** Admin start works fine")